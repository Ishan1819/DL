api:
  claude_api_key: "YOUR_CLAUDE_API_KEY"
  gemini_api_key: "AIzaqizLY"
  use_llm: "gemini"  # or "claude"

training:
  max_epochs: 5
  early_stopping_patience: 3
  num_optuna_trials: 5

paths:
  upload_dir: "data/uploads"
  models_dir: "data/models"
  plots_dir: "data/models/plots"
